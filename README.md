# Becca's Unbeatable Tic-Tac-Toe (in Clojure)

I bet you can't beat the computer!

### To play:

```
git clone git@github.com:beccanelson/clojure-tic-tac-toe.git
cd clojure-tic-tac-toe
lein run
```

### Run the specs:

`lein spec`
